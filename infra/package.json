{
  "name": "@probooks/infra",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "synth": "cdk synth --quiet",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint bin lib test",
    "test": "vitest run"
  },
  "dependencies": {
    "aws-cdk-lib": "2.258.0",
    "constructs": "10.6.0"
  },
  "devDependencies": {
    "@probooks/config": "workspace:*",
    "@types/node": "22.7.4",
    "aws-cdk": "2.1126.0",
    "eslint": "9.11.1",
    "ts-node": "10.9.2",
    "typescript": "5.6.2",
    "vitest": "2.1.1"
  }
}
